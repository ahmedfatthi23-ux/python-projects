import tkinter as tk

window = tk.Tk()
window.title("My First Game")

canvas = tk.Canvas(window, width=500, height=500)
canvas.pack()

# Make a small blue square
player = canvas.create_rectangle(100, 100, 110, 110, fill="blue")

window.mainloop()
